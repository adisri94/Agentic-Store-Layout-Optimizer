"""Service 1 — Affinity & Optimization.

Covers the MBA core plus enhancements #1, #4, #5, #6. Sprint 1 implements the
baseline Market Basket Analysis engine (``mba_core``). The governed public
interface (``get_recommendations``) is wired up in US-1.7 once the governance
service is in place; until then, mining is exposed via ``mba_core`` directly.
"""
